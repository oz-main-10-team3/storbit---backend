from django.apps import AppConfig


class StudyApplyConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.study_apply"
    label = "study_apply"  # 또는 name과 동일하게 'study_apply'
